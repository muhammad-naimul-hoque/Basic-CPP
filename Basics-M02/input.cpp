#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    // cin >> a >> b;
    cout << a << " " << b << endl;

    // string
    char ch[50];
    // cin.getline(ch, 50);
    cout << ch << endl;

    // array input
    int d[5];
    for (int i = 0; i < 5; i++)
    {
        cin >> d[i];
    }
    for (int i = 0; i < 5; i++)
    {
        cout << d[i] << " ";
    }

    return 0;
}